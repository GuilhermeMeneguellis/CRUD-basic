APP_PORT=3000
HOST_PORT=3002

MONGO_DATABASE=restaurant-api
MONGO_INITDB_ROOT_USERNAME=restaurant_user
MONGO_INITDB_ROOT_PASSWORD=change_this_password

# Use this only when running the API outside Docker.
MONGODB_URI=mongodb://restaurant_user:change_this_password@localhost:27017/restaurant-api?authSource=admin
